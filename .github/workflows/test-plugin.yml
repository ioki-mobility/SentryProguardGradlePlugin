name: 'Test Plugin'

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  pluginTest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        run: ./gradlew test